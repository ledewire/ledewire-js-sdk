{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "generate:types": {
      "inputs": ["../../ledewire.yml"],
      "outputs": ["src/api.gen.ts"],
      "cache": true
    },
    "build": {
      "dependsOn": ["^build", "generate:types"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "persistent": true,
      "cache": false
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "lint:fix": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["^build", "generate:types"]
    },
    "test": {
      "dependsOn": ["^build", "generate:types"],
      "env": ["LEDEWIRE_API_KEY", "LEDEWIRE_API_SECRET", "LEDEWIRE_BASE_URL"]
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "clean": {
      "cache": false
    }
  }
}
