{
  "name": "@ledewire/core",
  "version": "0.0.0",
  "private": true,
  "description": "Internal shared core for the LedeWire SDK — HTTP client, token manager, types, errors",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./test-utils": "./src/test-utils/index.ts"
  },
  "scripts": {
    "generate:types": "openapi-typescript ../../ledewire.yml --output src/api.gen.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "msw": "^2.7.0",
    "openapi-typescript": "^7.13.0",
    "openapi-typescript-helpers": "^0.1.0",
    "typescript": "*",
    "vitest": "^3.0.0"
  }
}
